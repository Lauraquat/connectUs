{% extends 'base.html.twig' %}

{% block title %}Edit Candidate{% endblock %}

{% block body %}

    <div class="containerIndex">
        <div class="titleAndButton">
            <h1 class="titleIndex">Edit your profile</h1>
            <a class="btn profileButton" href="{{ path('app_recruter_index') }}">back to list</a>
        </div>
        <h2 class="subtitleNewProfile" >Update your profile so that recruters can know you better  </h2>
        {{ include('candidate/_form.html.twig', {'button_label': 'Update'}) }}
 
    </div>
    
{#    {{ include('candidate/_delete_form.html.twig') }} //TODO put in profile view #}
{% endblock %}
