<form method="post" action="{{ path('app_recruter_delete', {'id': recruter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recruter.id) }}">
    <button class="btn deleteButton">Delete</button>
</form>
