{% extends 'base.html.twig' %}

{% block title %}{% trans %}Homepage{% endtrans %}{% endblock %}
{% block header%}{% endblock %}
{% block body %}

    <div class="homepageContainer" >
        <div class="homepageFirstSection">
            <div class="homepageRegister">
                <h1 class="homeTitle" >{% trans %}Welcome on <br /> ConnectUs{% endtrans %}</h1>
                <h2 class="homeSubtitle">{% trans %}Register or login to use the network <br /> which is just waiting for you !{% endtrans %}</h2>
                {% if is_granted('ROLE_CANDIDATE') %}
                    <a class="btn homepageButton" href="{{ path('app_recruter_index') }}">{% trans %}Show recruters{% endtrans %}</a>
                {% elseif is_granted('ROLE_RECRUTER') %}
                    <a class="btn homepageButton" href="{{ path('app_candidate_index') }}">{% trans %}Show candidates{% endtrans %}</a>
                {% else %}
                    <a class="btn homepageButton" href="{{ path("app_register")}}">{% trans %}Register{% endtrans %}</a>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                    <a class="btn homepageButton" href="{{ path('app_logout') }}">{% trans %}Logout{% endtrans %}</a>
                {% else %}
                    <a class="btn homepageButton" href="{{ path('app_login') }}">{% trans %}Login{% endtrans %}</a>
                {% endif %}
            </div>
            <img class="homePicture" src="{{ asset('images/home.jpg') }}" alt="Logo"/>
        </div>

        <div class="homepageSecondSection">
            <h2 class="homepageSecondSectionSubtitle">{% trans %}What is ConnectUs ?{% endtrans %}</h2>
            <p class="homepageInformation">
                {% trans %}ConnectUs allows companies to find new employees. As a company you can look at the <br /> profiles of people with a candidate profile. On these profiles you can see general informations, disponibility and CV.{% endtrans %}
            </p>
            <p class="homepageInformation">
                {% trans %}If you are looking for jobs, with ConnectUs, you have access to a multitude of profiles companies and the jobs they offer.{% endtrans %}
            </p>

            <p class="homepageInformation">
                {% trans %}Whether you are a recruiter or someone looking for jobs, you can show your interest <br />for a profile and you may be contacted by the company or candidate you need !{% endtrans %}
            </p>
        </div>
    </div>

{% endblock %}
